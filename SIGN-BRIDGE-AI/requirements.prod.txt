# SignBridge AI - Production Requirements (Optimized for Railway)
# ================================================================
# This is a SLIMMED DOWN version for production deployment.
# Key changes from requirements.txt:
#   - tflite-runtime instead of tensorflow (50MB vs 2GB!)
#   - opencv-python-headless instead of opencv-python (no GUI)
#   - Removed: gTTS, pygame, scikit-learn, matplotlib, SpeechRecognition,
#              moviepy, beautifulsoup4, pyttsx3, pysrt, pytube, PyAudio
#
# If you need full training capabilities, use requirements.txt locally.

# --- Core ML (TFLite only, no full TensorFlow) ---
tflite-runtime==2.14.0
numpy==2.1.3
mediapipe==0.10.11
protobuf==3.20.3

# --- Computer Vision (headless = no GUI libs) ---
opencv-python-headless==4.13.0.92

# --- Web Framework ---
Flask==3.1.3
Flask-SocketIO==5.6.1
flask-cors==5.0.1
flask-jwt-extended==4.7.1
Werkzeug==3.1.3
python-socketio==5.11.0  # Required by Flask-SocketIO
eventlet==0.35.2         # Async worker for gunicorn

# --- Authentication ---
bcrypt==4.3.0
PyJWT==2.10.1
google-auth==2.38.0

# --- Server ---
gunicorn==23.0.0
